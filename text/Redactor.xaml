<Window x:Class="text.Redactor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:text"
        mc:Ignorable="d"
        Title="Redactor" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="table" Grid.Row="2" Grid.Column="0" SelectionChanged="table_SelectionChanged" BorderBrush="white" Background="white" Foreground="Black" FontFamily="Arial"/>
        
        <Border BorderBrush="Black" BorderThickness="1" Margin="50,50,50,50" CornerRadius="20,20,20,20" Grid.Row="0">
            <Button x:Name="add_que" Content="Добавить тест" Background="LightCoral" FontSize="18" FontFamily="Arial Black" Click="add_que_Click" Grid.Row="1"  VerticalAlignment="Center" Height="120" Margin="10,10,10,10" />
        </Border>

    </Grid>
</Window>
